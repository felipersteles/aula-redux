import { Card } from "../Card";

export const Media = (props) => {
  return (
    <Card title="Média" blue>
      <div>
        <span>
          <span>Resultado: </span>
          <strong>{5}</strong>
        </span>
      </div>
    </Card>
  );
};
