import { Card } from "../Card";

export const Sorteio = (props) => {
  return (
    <Card title="Sorteio" purple>
      <div className="intervalo">
        <span>
          <span>Resultado: </span>
          <strong>{7}</strong>
        </span>
      </div>
    </Card>
  );
};
